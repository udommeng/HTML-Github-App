name: GitHub Actions Demo

on:
  push:
    branches:
      - main

  pull_request:
    branches:
      - main

jobs:
  build-job:
    runs-on: ubuntu-latest
    steps:
      - run: echo "buildjob"

  test-job:
    runs-on: ubuntu-latest
    steps:
      - run: echo "test job"

  deploy-job:
    runs-on: ubuntu-latest
    steps:
      - run: echo "deploy job to production server"
